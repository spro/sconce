{
  "name": "sconce-data",
  "scripts": {
    "start": "coffee service.coffee",
    "restart": "pm2 restart sconce:data || pm2 start --name sconce:data service.coffee"
  },
  "dependencies": {
    "data-service": "git+ssh://git@github.com/prontotype-us/data-service.git",
    "somata": "^0.2.5"
  }
}
